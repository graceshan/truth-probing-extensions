# Analysis + extraction deps. Install a CPU build locally; on Runpod install the
# GPU torch wheel matching the box's CUDA (see CLAUDE.md) — do not pin CUDA here.
torch==2.11.0
transformers==5.12.1
scikit-learn==1.9.0
scipy==1.17.1            # src/stats.py seed_mean_ci: t-distribution CI (scipy.stats.t.ppf) for the Δ interval
pandas==3.0.3
numpy==2.4.6
matplotlib==3.11.1
