# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/

# Activation dumps: never commit the arrays; DO keep the parallel label CSVs
# (acts/{dataset}.csv) so a checkpoint stays interpretable without the .npy.
acts/*.npy
*.npy
*.pt
*.pkl

# Upstream data repo — cloned as a sibling, not vendored. Safeguard in case it
# ever gets cloned inside this repo: never track the whole third-party repo.
/Truth_is_Universal/

# OS / editor
.DS_Store
.vscode/
